# TZ-BOT-REDESIGN-2026 — Редизайн UX чат-бота
> Концепция: бот = пульт управления, сайт = данные
> Дата: 2026-05-14

---

## КОНЦЕПЦИЯ

Бот не показывает большие списки. Бот управляет настройками и запускает действия.
Детальная работа с постами — на сайте.

---

## ЭКРАН 1: ГЛАВНОЕ МЕНЮ КАНАЛА (screenChannel)

**Было:** статус + 6 кнопок навигации
**Стало:**

```
📢 @seorezult · Путь татарина: SEO

📊 В поиске: 12 постов
👁 Просмотров за 30 дней: 340
👆 Переходов в Telegram: 23

🟢 Автопубликация: ВКЛ
```

Кнопки:
```
[📄 Публикации]
[🟢 Автопубликация: ВКЛ]   ← toggle, меняет статус прямо здесь
[🚀 Открыть панель]         ← ссылка с loginToken
[📊 Аналитика]
[⚙️ Настройки]
[‹ К каналам]
```

Если `!botIsAdmin` — добавить кнопку:
```
[🔄 Проверить — бот добавлен]
```

**Реализация:**
- Статистику брать из `analyticsEvents` (уже есть в `screenAnalytics`)
- Кол-во постов в поиске: `SELECT COUNT(*) FROM posts WHERE channel_id=... AND is_indexed=true`
- Toggle автопубликации — обновляет БД и перерисовывает экран

---

## ЭКРАН 2: ПУБЛИКАЦИИ (screenPosts) — ПОЛНЫЙ РЕДИЗАЙН

**Было:** список 10 постов с пагинацией, фильтры, кнопки продвинуть
**Стало:** умный экран с мульти-выбором

### Состояние по умолчанию (ничего не выбрано):

```
📄 Публикации @seorezult

✅ В поиске: 12
⚡ Ждут продвижения: 47
⚫ Короткие (не подходят): 8

— Последние 5 новых постов —
☐ Почему SEO не может стоить...
☐ Кейс - Омоложение
☐ Анализ конкурентов
☐ А вот и ссылки - Анализ конку...
☐ Аудит промышленной безопасности
```

Кнопки:
```
[☐ Почему SEO не может...] → ch:sel:{channelId}:{postId}
[☐ Кейс - Омоложение]     → ch:sel:{channelId}:{postId}
[☐ Анализ конкурентов]    → ch:sel:{channelId}:{postId}
[☐ А вот и ссылки...]     → ch:sel:{channelId}:{postId}
[☐ Аудит безопасности...] → ch:sel:{channelId}:{postId}
[⚡ Продвинуть все 47]     → ch:promote_all:{channelId}
[🌐 Все публикации на сайте →]  ← URL с loginToken
[‹ К каналу]
```

### После выбора 2 постов:

```
📄 Публикации @seorezult
Выбрано: 2 поста

✅ Почему SEO не может стоить...
☐ Кейс - Омоложение
✅ Анализ конкурентов
☐ А вот и ссылки...
☐ Аудит безопасности...
```

Кнопки:
```
[✅ Почему SEO не может...]  → ch:sel:{channelId}:{postId}  (снять выбор)
[☐ Кейс - Омоложение]
[✅ Анализ конкурентов]      (снять выбор)
[☐ А вот и ссылки...]
[☐ Аудит безопасности...]
[⚡ Продвинуть выбранные (2)] → ch:promote_selected:{channelId}:{id1},{id2}
[⚡ Продвинуть все 47]
[🌐 Все публикации на сайте →]
[‹ К каналу]
```

---

## ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Хранение выбора
Выбранные посты хранятся в сессии пользователя:
```typescript
// В bot-sessions: добавить поле selected_posts: number[]
sess.data.selected_posts = [postId1, postId2]
```

### Callback data для toggle
```
ch:sel:{channelId}:{postId}
```
- Если postId уже в `sess.data.selected_posts` → убрать
- Если нет → добавить
- Перерисовать экран с обновлёнными ✅/☐

### Кнопка "Продвинуть выбранные"
```
ch:promote_selected:{channelId}
```
- Берёт список из `sess.data.selected_posts`
- Отправляет каждый в worker `/api/posts/{id}/promote`
- Очищает `sess.data.selected_posts`
- Показывает: "⚡ Запущено N постов!"

### Отображение постов
- Брать последние 5 непродвинутых: `seo_slug IS NULL AND text_length >= 100 ORDER BY date DESC LIMIT 5`
- Очищать текст: убрать `\n`, `#хэштеги`, эмодзи, обрезать до 35 символов

### "Открыть панель на сайте"
```typescript
const loginUrl = `${SITE_URL}/auth/sign-in?token=${loginToken}&redirect=/dashboard/channels/${channelId}/posts`
```

---

## ЭКРАН 3: НАСТРОЙКИ (screenSettings) — УПРОЩЕНИЕ

**Убрать из бота:** кнопки задержки (0/10м/30м/1ч/2ч) — слишком детально для бота.

**Оставить только:**
```
⚙️ Настройки @seorezult

Автопубликация: 🟢 ВКЛ
Задержка: 30 мин

[🟢 Автопубликация: ВКЛ]  ← toggle
[⏱ Изменить задержку]     ← открывает выбор задержки
[🗑 Удалить канал]
[‹ К каналу]
```

---

## ФАЙЛЫ КОТОРЫЕ МЕНЯЮТСЯ

- `frontend/src/app/api/webhook/telegram/route.ts`:
  - `screenChannel` — добавить статистику, toggle автопубл
  - `screenPosts` — полный редизайн с мульти-выбором
  - `screenSettings` — упрощение
  - Новые handlers: `ch:sel:*`, `ch:promote_selected:*`
- `frontend/src/lib/bot-sessions.ts` — добавить `selected_posts` в тип сессии

---

## ЧТО НЕ МЕНЯЕТСЯ

- Онбординг (/start → добавить канал → активация) — уже хорошо работает
- Уведомления (пост опубликован, SEO-аудит готов)
- Главное меню (`screenMain`)
- Аналитика (`screenAnalytics`) — переносим данные в `screenChannel`
